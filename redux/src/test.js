MyComponent.propTypes = {
  // contents...
}

export default function MyComponent({ prop1, proop2 }) {

}

const COLUMNES = [
  { id: 1, name: 'kim' },
  { id: 2, name: 'lee' },
]

const URL_PRODUCT_LIST = '/api/product';

function getTotalPrice({ price, total }) {
  
}